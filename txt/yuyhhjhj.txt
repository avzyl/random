import java.io.*;
import java.util.ArrayList;
import java.util.Scanner;

public class Main {
    public static void main(String[] args) throws IOException {
        ArrayList<String> users = new ArrayList<>();
        int counter =0;
        // Prompt the user for their username and name and record in to the file
        Scanner scanner = new Scanner(System.in);
        while (counter < 10) {
            System.out.print("Enter your username: ");
            String username = scanner.nextLine();
            System.out.print("Enter your name: ");
            String name = scanner.nextLine();

            // Add the username and name to the list of users
            users.add(username + "," + name);
            counter++;
        }
        // Create a new file to store the list of users
        File file = new File("users.txt");
        if (!file.exists()) {
            file.createNewFile();
        }

        // Write the list of users to the file
        FileWriter fileWriter = new FileWriter(file);
        for (String user : users) {
            fileWriter.write(user + System.lineSeparator());
        }
        fileWriter.close();

        //Admin Function
        System.out.println("Welcome Admin, please enter 1 to activate user and 2 to deactivate user");
        int adminChoice = scanner.nextInt();
        scanner.nextLine();
        if(adminChoice == 1){
            System.out.print("Enter the username of user to activate : ");
            String userToActivate = scanner.nextLine();
            activateUser(userToActivate,users);
        }else if(adminChoice == 2){
            System.out.print("Enter the username of user to deactivate : ");
            String userToDeactivate = scanner.nextLine();
            deactivateUser(userToDeactivate,users);
