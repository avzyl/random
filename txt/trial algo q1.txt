Start
get birth_year
while age = 2022 - birth_year
	if birth_year > current_year, then:
	   print "User's age exceeds the current year. Kindly enter a valid birth year."
	elif birth_year < 1962, then:
	   print "User's age exceeds 60 years. Kindly enter a valid birth year."
	else:
	year60 = computeYear60()
	days = computeDays()
	weeks = computeWeeks()
	months = computeMonths()
	years = computeYears()
	   print "You only have {365*age1} days, {52*age1} weeks, {12*age1} months, and {1*age1} years before you turn 60."
	endif
endfor
end

computeYear60():
	set current_year = 2022
	set target_age = 60
	year60 = current_year + target_age
return year60

computeDays():
	set one_year = 365.2425
	days = year60 * one_year
return days

computeWeeks():
	set one_week = 7
	weeks = days/one_week
return weeks

computeMonths():
	set one_month = 30.4167
	months = days/months
return months

computeYears():
	years = year60 - current_year